<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
</head>
<body>

<h1>🖥 Лабораторная работа №1: Введение в PHP</h1>

<h2>🎯 Цель работы</h2>
<p>Познакомиться с основами PHP, научиться запускать скрипты, выводить данные и работать с переменными.</p>

<hr>

<h2>📁 Шаг 1. Создание проекта</h2>
<p>Создана директория проекта:</p>
<pre><code>D:\Projects\PHP\01_Introduction</code></pre>
<p>В ней создан файл <code>index.php</code>.</p>

<hr>

<h2>📝 Шаг 2. Первая PHP-программа</h2>
<p>В файл <code>index.php</code> добавлен код:</p>
<pre><code>&lt;?php
echo "Привет, мир!";
</code></pre>

<h3>🔹 Запуск программы</h3>
<p><strong>Через встроенный сервер PHP:</strong></p>
<pre><code>cd D:\Projects\PHP\01_Introduction
php -S localhost:8000
</code></pre>
<p>Открыть в браузере: <code>http://localhost:8000</code></p>

<p><strong>Через XAMPP:</strong></p>
<p>Поместить папку проекта в <code>C:\xampp\htdocs\</code> и открыть: <code>http://localhost/01_Introduction</code></p>

<hr>

<h2>💻 Шаг 3. Вывод данных</h2>
<p>Вывод текста с помощью <strong>echo</strong> и <strong>print</strong>:</p>
<pre><code>&lt;?php
echo "Hello, World with echo!&lt;br />";
print "Hello, World with print!&lt;br />";
</code></pre>

<hr>

<h2>🔹 Шаг 4. Работа с переменными</h2>
<p>Созданы переменные:</p>
<pre><code>&lt;?php
$days = 288;
$message = "Все возвращаются на работу!";
</code></pre>

<h3>Способы вывода</h3>
<p><strong>1. Конкатенация:</strong></p>
<pre><code>echo "Количество дней: " . $days . "&lt;br />";
echo $message . "&lt;br />";
</code></pre>

<p><strong>2. В двойных кавычках:</strong></p>
<pre><code>echo "Количество дней: $days &lt;br />";
echo "$message &lt;br />";
</code></pre>

<hr>

<h2>📄 Итоговый код index.php</h2>
<pre><code>&lt;?php

echo "Привет, мир!&lt;br />&lt;br />";

echo "Hello, World with echo!&lt;br />";
print "Hello, World with print!&lt;br />&lt;br />";

$days = 288;
$message = "Все возвращаются на работу!";

echo "Количество дней: " . $days . "&lt;br />";
echo $message . "&lt;br />&lt;br />";

echo "Количество дней: $days &lt;br />";
echo "$message &lt;br />";
</code></pre>

<hr>

<h2>❓ Контрольные вопросы</h2>

<h3>1️⃣ Какие способы установки PHP существуют?</h3>
<ul>
    <li>Установка с официального сайта PHP</li>
    <li>Готовые сборки: XAMPP, OpenServer</li>
    <li>Docker-контейнеры</li>
</ul>

<h3>2️⃣ Как проверить, что PHP установлен и работает?</h3>
<p>В командной строке:</p>
<pre><code>php -v</code></pre>
<p>Также можно создать файл <code>phpinfo.php</code>:</p>
<pre><code>&lt;?php
phpinfo();
</code></pre>

<h3>3️⃣ Чем отличается echo от print?</h3>
<table border="1" cellpadding="5" cellspacing="0">
<tr>
<th>echo</th>
<th>print</th>
</tr>
<tr>
<td>Быстрее</td>
<td>Медленнее</td>
</tr>
<tr>
<td>Можно выводить несколько значений</td>
<td>Только одно значение</td>
</tr>
<tr>
<td>Не возвращает значение</td>
<td>Возвращает 1</td>
</tr>
</table>

<hr>

<h2>✅ Вывод</h2>
<p>В ходе лабораторной работы изучены основы PHP:</p>
<ul>
    <li>Запуск скриптов</li>
    <li>Вывод данных с помощью <code>echo</code> и <code>print</code></li>
    <li>Работа с переменными и их вывод на экран</li>
</ul>

</body>
</html>
